# KeepCoding-IA-ML
Repositorio de Entrega de la Práctica de Machine Learning

# Instalación del Ambiente

```
uv venv
source .venv/bin/activate
uv sync
uv run python -m ipykernel install --user --name=keepcoding
uv add --dev uv

jupyter-lab
```